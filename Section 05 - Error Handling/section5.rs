// Welcome to the interactive Rust file for Section 5!
// You can use this file to practice Rust concepts as you learn them.
//
// To run this file, make sure you have Rust and Cargo installed.
// Navigate to the directory containing this file in your terminal,
// and run the command 'cargo run --bin section5' (without quotes) to execute the code.

fn main() {
    // 1. Define a function `divide` that takes two f64 parameters and returns a Result<f64, String>.
    // If the second parameter is zero, return an error with the message "Cannot divide by zero".
    // Otherwise, return the result of the division.
    // Your code here

    // 2. Call the `divide` function with the arguments 10.0 and 2.0, and handle the Result using a match expression.
    // Your code here

    // 3. Define a function `find_even_number` that takes a slice of i32 and returns an Option<i32>.
    // If there is an even number in the slice, return it wrapped in Some(). If not, return None.
    // Your code here

    // 4. Call the `find_even_number` function with an array of odd numbers and handle the Option using a match expression.
    // Your code here
}
